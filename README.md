# banksystem
Bank Management System is a C++ console application built using OOP principles that simulates real banking operations. It supports multiple roles (Client, Employee, Admin) with features like account management, transactions, and file-based data storage.
